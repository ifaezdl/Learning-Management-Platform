# تحلیل و توضیح تابع `groupBySkill`

## 1. هدف تابع چیست؟

تابع `groupBySkill` برای **تحلیل عملکرد دانشجو بر اساس مهارت‌های مختلف** استفاده می‌شود.

فرض کنیم سؤالات یک آزمون دارای Skill Tag باشند.

مثلاً:

```text
سؤال 1 → JavaScript
سؤال 2 → JavaScript
سؤال 3 → React
سؤال 4 → React
سؤال 5 → TypeScript
```

و برای هر سؤال مشخص باشد که دانشجو پاسخ درست داده یا غلط.

این تابع پاسخ‌ها را بر اساس `skillTag` گروه‌بندی می‌کند و برای هر مهارت مشخص می‌کند:

- چند سؤال مربوط به آن مهارت بوده است؟
- چند سؤال را درست پاسخ داده است؟
- درصد موفقیت دانشجو در آن مهارت چقدر بوده است؟

در نهایت مهارت‌ها را از **ضعیف‌ترین به قوی‌ترین** مرتب می‌کند.

---

# 2. تعریف تابع

تابع به شکل زیر تعریف شده است:

```ts
groupBySkill(
  answers: {
    skillTag: string | null | undefined;
    isCorrect: boolean;
  }[],
): SkillStat[] {
```

یعنی تابع یک آرایه از پاسخ‌های دانشجو دریافت می‌کند.

هر پاسخ شامل دو اطلاعات اصلی است:

```ts
{
  skillTag: string | null | undefined;
  isCorrect: boolean;
}
```

---

# 3. ورودی تابع چیست؟

ورودی تابع `answers` است.

مثلاً:

```ts
[
  {
    skillTag: 'JavaScript',
    isCorrect: true
  },
  {
    skillTag: 'JavaScript',
    isCorrect: false
  },
  {
    skillTag: 'React',
    isCorrect: true
  }
]
```

یعنی سه سؤال داریم:

```text
سؤال 1 → JavaScript → درست
سؤال 2 → JavaScript → غلط
سؤال 3 → React     → درست
```

---

# 4. `skillTag` چیست؟

`skillTag` مشخص می‌کند سؤال مربوط به چه مهارتی است.

مثلاً:

```text
JavaScript
React
TypeScript
SQL
CSS
HTML
```

در سیستم آموزشی می‌توان از Skill Tag برای مشخص کردن موضوع یا مهارت مورد سنجش سؤال استفاده کرد.

مثلاً:

```text
سؤال:
"کدام متد برای تبدیل آرایه به یک مقدار استفاده می‌شود؟"

Skill Tag:
JavaScript - Array Methods
```

بنابراین سیستم می‌تواند بعداً بفهمد دانشجو در کدام مهارت بهتر یا ضعیف‌تر عمل کرده است.

---

# 5. `isCorrect` چیست؟

`isCorrect` مشخص می‌کند که پاسخ دانشجو درست بوده یا خیر.

این مقدار یک Boolean است:

```ts
true
```

یا:

```ts
false
```

مثلاً:

```ts
{
  skillTag: 'React',
  isCorrect: true
}
```

یعنی:

> دانشجو به یک سؤال مربوط به React پاسخ درست داده است.

و:

```ts
{
  skillTag: 'React',
  isCorrect: false
}
```

یعنی:

> دانشجو به یک سؤال مربوط به React پاسخ اشتباه داده است.

---

# 6. خروجی تابع چیست؟

تابع در نهایت یک آرایه از نوع:

```ts
SkillStat[]
```

برمی‌گرداند.

هر آیتم خروجی تقریباً به این شکل است:

```ts
{
  tag: string,
  correct: number,
  total: number,
  percentage: number
}
```

مثلاً:

```ts
{
  tag: 'React',
  correct: 6,
  total: 10,
  percentage: 60
}
```

یعنی:

> دانشجو از 10 سؤال مربوط به React، به 6 سؤال پاسخ درست داده و درصد موفقیت او 60٪ بوده است.

---

# 7. مرحله اول: ساخت Map

در ابتدای تابع داریم:

```ts
const map = new Map<string, { correct: number; total: number }>();
```

این `Map` برای ذخیره آمار هر مهارت استفاده می‌شود.

ساختار آن به صورت مفهومی:

```text
Map

JavaScript
    ↓
correct: 5
total: 8

React
    ↓
correct: 3
total: 6

TypeScript
    ↓
correct: 7
total: 10
```

---

# 8. چرا از Map استفاده شده است؟

چون می‌خواهیم برای هر `skillTag` یک دسته یا Bucket داشته باشیم.

مثلاً:

```text
JavaScript → آمار JavaScript
React      → آمار React
SQL        → آمار SQL
```

اگر یک سؤال جدید مربوط به JavaScript باشد، به جای ایجاد یک مورد جدید، آمار JavaScript را افزایش می‌دهیم.

---

# 9. حلقه اصلی تابع

کد:

```ts
for (const a of answers) {
```

یعنی تابع تک‌تک پاسخ‌های دانشجو را بررسی می‌کند.

اگر 20 پاسخ داشته باشیم:

```text
پاسخ 1
↓
پاسخ 2
↓
پاسخ 3
↓
...
↓
پاسخ 20
```

هر پاسخ یک بار بررسی می‌شود.

---

# 10. مرحله بعد: مشخص کردن Skill Tag

کد:

```ts
const tag =
  a.skillTag && a.skillTag.trim() ? a.skillTag.trim() : 'سایر';
```

این قسمت بسیار مهم است.

تابع بررسی می‌کند که آیا `skillTag` معتبر است یا خیر.

---

# 11. چرا `trim()` استفاده شده است؟

فرض کنیم مقدار Skill Tag این باشد:

```text
"  JavaScript  "
```

اگر مستقیماً از آن استفاده کنیم، فاصله‌های اضافی وجود دارند.

با:

```ts
a.skillTag.trim()
```

به:

```text
"JavaScript"
```

تبدیل می‌شود.

بنابراین `trim()` فاصله‌های ابتدا و انتهای متن را حذف می‌کند.

---

# 12. اگر Skill Tag خالی باشد چه می‌شود؟

ممکن است:

```ts
skillTag = null
```

یا:

```ts
skillTag = undefined
```

باشد.

همچنین ممکن است رشته خالی باشد:

```ts
skillTag = ''
```

یا حتی فقط فاصله باشد:

```text
"     "
```

در این شرایط تابع:

```text
سایر
```

را به عنوان Skill Tag قرار می‌دهد.

بنابراین:

```text
skillTag معتبر
↓
خود Skill Tag

skillTag نامعتبر یا خالی
↓
"سایر"
```

---

# 13. مثال Skill Tag خالی

اگر داشته باشیم:

```ts
{
  skillTag: null,
  isCorrect: true
}
```

تابع آن را در گروه:

```text
سایر
```

قرار می‌دهد.

همین اتفاق برای:

```ts
undefined
```

و:

```ts
''
```

نیز رخ می‌دهد.

---

# 14. پیدا کردن Bucket مربوط به Skill

بعد از مشخص شدن `tag`، این خط اجرا می‌شود:

```ts
const bucket = map.get(tag) ?? { correct: 0, total: 0 };
```

اینجا تابع بررسی می‌کند:

> آیا قبلاً برای این Skill یک آمار ساخته‌ایم؟

---

# 15. اگر Skill قبلاً وجود داشته باشد

فرض کنیم Map این باشد:

```text
JavaScript
correct = 3
total = 5
```

و پاسخ جدید هم مربوط به JavaScript باشد.

در این حالت:

```ts
map.get('JavaScript')
```

همان Bucket قبلی را برمی‌گرداند:

```ts
{
  correct: 3,
  total: 5
}
```

---

# 16. اگر Skill برای اولین بار دیده شود

فرض کنیم اولین سؤال مربوط به:

```text
React
```

باشد.

در این حالت:

```ts
map.get('React')
```

چیزی پیدا نمی‌کند.

پس بخش بعد از `??` اجرا می‌شود:

```ts
{
  correct: 0,
  total: 0
}
```

یعنی یک Bucket جدید ساخته می‌شود.

---

# 17. مفهوم `??`

این قسمت:

```ts
map.get(tag) ?? { correct: 0, total: 0 }
```

یعنی:

> اگر برای این Skill مقدار قبلی وجود داشت، همان را استفاده کن؛ در غیر این صورت یک آمار جدید با مقدار صفر بساز.

به زبان ساده:

```text
آیا Skill قبلاً وجود دارد؟
       ↓
     بله
       ↓
استفاده از آمار قبلی

     خیر
       ↓
correct = 0
total = 0
```

---

# 18. افزایش تعداد کل سؤالات

بعد از پیدا کردن Bucket:

```ts
bucket.total += 1;
```

اجرا می‌شود.

یعنی:

> یک سؤال دیگر به این Skill اضافه شد.

مثلاً اگر قبلاً:

```text
total = 3
```

بود:

```text
total = 4
```

می‌شود.

---

# 19. بررسی درست بودن پاسخ

بعد:

```ts
if (a.isCorrect) bucket.correct += 1;
```

اگر پاسخ درست باشد:

```ts
isCorrect = true
```

مقدار `correct` یکی افزایش پیدا می‌کند.

مثلاً:

```text
correct = 3
```

تبدیل می‌شود به:

```text
correct = 4
```

اما اگر:

```ts
isCorrect = false
```

باشد، `correct` تغییری نمی‌کند.

---

# 20. در نهایت ذخیره Bucket در Map

آخر حلقه:

```ts
map.set(tag, bucket);
```

این خط آمار مربوط به Skill را در Map ذخیره می‌کند.

مثلاً:

```text
JavaScript
↓
correct = 4
total = 6
```

---

# 21. روند کامل حلقه

برای هر پاسخ، این اتفاق می‌افتد:

```text
دریافت پاسخ
     ↓
بررسی Skill Tag
     ↓
اگر خالی بود → "سایر"
     ↓
پیدا کردن Bucket
     ↓
اگر وجود نداشت → ایجاد Bucket جدید
     ↓
افزایش total
     ↓
اگر پاسخ درست بود → افزایش correct
     ↓
ذخیره در Map
```

---

# 22. مرحله دوم: ساخت خروجی نهایی

بعد از اینکه تمام پاسخ‌ها بررسی شدند:

```ts
const result: SkillStat[] = [];
```

یک آرایه خالی به نام `result` ساخته می‌شود.

این آرایه قرار است نتیجه نهایی تحلیل را نگه دارد.

---

# 23. پیمایش Map

بعد:

```ts
for (const [tag, { correct, total }] of map.entries()) {
```

تابع تمام Skillهایی که در Map ذخیره شده‌اند را بررسی می‌کند.

مثلاً:

```text
JavaScript
React
TypeScript
SQL
```

---

# 24. ساخت خروجی برای هر Skill

برای هر Skill این object ساخته می‌شود:

```ts
result.push({
  tag,
  correct,
  total,
  percentage: total === 0 ? 0 : Math.round((correct / total) * 100),
});
```

خروجی شامل چهار مقدار است:

```text
tag
correct
total
percentage
```

---

# 25. `tag`

نام مهارت است.

مثلاً:

```text
JavaScript
```

---

# 26. `correct`

تعداد پاسخ‌های درست است.

مثلاً:

```text
correct = 7
```

یعنی دانشجو 7 سؤال را درست پاسخ داده است.

---

# 27. `total`

تعداد کل سؤال‌های آن Skill است.

مثلاً:

```text
total = 10
```

یعنی 10 سؤال مربوط به این مهارت وجود داشته است.

---

# 28. محاسبه درصد

مهم‌ترین قسمت این بخش:

```ts
Math.round((correct / total) * 100)
```

فرمول:

```text
percentage = (correct / total) × 100
```

مثلاً:

```text
correct = 7
total = 10
```

پس:

```text
percentage = (7 / 10) × 100
```

بنابراین:

```text
percentage = 70%
```

---

# 29. چرا از `Math.round()` استفاده شده است؟

فرض کنیم:

```text
correct = 2
total = 3
```

محاسبه:

```text
2 / 3 × 100
```

برابر تقریباً:

```text
66.666...
```

است.

اما با:

```ts
Math.round()
```

نتیجه:

```text
67
```

می‌شود.

بنابراین درصد نهایی یک عدد صحیح خواهد بود.

---

# 30. چرا `total === 0` بررسی شده است؟

کد:

```ts
total === 0 ? 0 : Math.round((correct / total) * 100)
```

یعنی اگر تعداد سؤال‌ها صفر باشد، درصد را:

```text
0
```

قرار بده.

در غیر این صورت:

```text
(correct / total) × 100
```

را محاسبه کن.

این کار از تقسیم بر صفر جلوگیری می‌کند.

---

# 31. مفهوم شرط سه‌تایی

این قسمت:

```ts
total === 0 ? 0 : Math.round((correct / total) * 100)
```

یک **Ternary Operator** است.

معادل:

```ts
if (total === 0) {
  percentage = 0;
} else {
  percentage = Math.round((correct / total) * 100);
}
```

است.

---

# 32. مرحله آخر: مرتب‌سازی از ضعیف‌ترین به قوی‌ترین

در انتهای تابع داریم:

```ts
result.sort((a, b) => a.percentage - b.percentage);
```

این خط خروجی را بر اساس درصد مرتب می‌کند.

از:

```text
کمترین درصد
```

به:

```text
بیشترین درصد
```

مثلاً اگر:

```text
JavaScript → 80%
React      → 45%
TypeScript → 70%
SQL        → 30%
```

باشد، نتیجه:

```text
SQL        → 30%
React      → 45%
TypeScript → 70%
JavaScript → 80%
```

خواهد بود.

---

# 33. چرا از ضعیف‌ترین به قوی‌ترین مرتب می‌کنیم؟

چون هدف این تابع علاوه بر تحلیل عملکرد، پیدا کردن **نقاط ضعف دانشجو** است.

وقتی ضعیف‌ترین Skill در ابتدای آرایه قرار بگیرد، سیستم به راحتی می‌تواند آن را به عنوان مهارت نیازمند توجه شناسایی کند.

مثلاً:

```text
SQL        → 30%
React      → 45%
TypeScript → 70%
JavaScript → 80%
```

در این حالت مشخص است که:

```text
ضعیف‌ترین مهارت = SQL
```

و:

```text
قوی‌ترین مهارت = JavaScript
```

---

# 34. خروجی نهایی

در نهایت:

```ts
return result;
```

آرایه تحلیل‌شده را برمی‌گرداند.

---

# 35. مثال کامل

حالا کل تابع را با یک مثال واقعی بررسی می‌کنیم.

فرض کنیم دانشجو به 8 سؤال پاسخ داده است:

| سؤال | Skill Tag | پاسخ |
|---|---|---|
| 1 | JavaScript | درست |
| 2 | JavaScript | غلط |
| 3 | JavaScript | درست |
| 4 | React | غلط |
| 5 | React | درست |
| 6 | React | غلط |
| 7 | SQL | درست |
| 8 | SQL | درست |

پس ورودی تابع:

```ts
const answers = [
  {
    skillTag: 'JavaScript',
    isCorrect: true,
  },
  {
    skillTag: 'JavaScript',
    isCorrect: false,
  },
  {
    skillTag: 'JavaScript',
    isCorrect: true,
  },
  {
    skillTag: 'React',
    isCorrect: false,
  },
  {
    skillTag: 'React',
    isCorrect: true,
  },
  {
    skillTag: 'React',
    isCorrect: false,
  },
  {
    skillTag: 'SQL',
    isCorrect: true,
  },
  {
    skillTag: 'SQL',
    isCorrect: true,
  },
];
```

---

# 36. شروع Map

در ابتدا:

```ts
const map = new Map();
```

Map خالی است:

```text
{}
```

---

# 37. بررسی سؤال اول

سؤال اول:

```text
JavaScript → درست
```

چون JavaScript هنوز در Map وجود ندارد:

```text
correct = 0
total = 0
```

ساخته می‌شود.

سپس:

```text
total += 1
```

پس:

```text
total = 1
```

چون پاسخ درست است:

```text
correct += 1
```

پس:

```text
correct = 1
```

Map:

```text
JavaScript
correct = 1
total = 1
```

---

# 38. بررسی سؤال دوم

سؤال دوم:

```text
JavaScript → غلط
```

JavaScript قبلاً وجود دارد:

```text
correct = 1
total = 1
```

یک سؤال جدید اضافه می‌کنیم:

```text
total = 2
```

اما چون پاسخ غلط است:

```text
correct = 1
```

پس:

```text
JavaScript
correct = 1
total = 2
```

---

# 39. بررسی سؤال سوم

سؤال سوم:

```text
JavaScript → درست
```

قبل از سؤال:

```text
correct = 1
total = 2
```

بعد از اضافه شدن سؤال:

```text
total = 3
```

چون درست است:

```text
correct = 2
```

پس:

```text
JavaScript
correct = 2
total = 3
```

---

# 40. بررسی سؤال چهارم

سؤال چهارم:

```text
React → غلط
```

React هنوز وجود ندارد.

پس:

```text
correct = 0
total = 0
```

ایجاد می‌شود.

بعد:

```text
total = 1
```

چون پاسخ غلط است:

```text
correct = 0
```

پس:

```text
React
correct = 0
total = 1
```

---

# 41. بررسی سؤال پنجم

```text
React → درست
```

قبل:

```text
correct = 0
total = 1
```

بعد:

```text
total = 2
correct = 1
```

پس:

```text
React
correct = 1
total = 2
```

---

# 42. بررسی سؤال ششم

```text
React → غلط
```

قبل:

```text
correct = 1
total = 2
```

بعد:

```text
total = 3
correct = 1
```

پس:

```text
React
correct = 1
total = 3
```

---

# 43. بررسی سؤال هفتم

```text
SQL → درست
```

SQL جدید است:

```text
correct = 0
total = 0
```

بعد:

```text
total = 1
correct = 1
```

پس:

```text
SQL
correct = 1
total = 1
```

---

# 44. بررسی سؤال هشتم

```text
SQL → درست
```

قبل:

```text
correct = 1
total = 1
```

بعد:

```text
total = 2
correct = 2
```

پس:

```text
SQL
correct = 2
total = 2
```

---

# 45. وضعیت نهایی Map

بعد از بررسی تمام 8 سؤال:

```text
JavaScript
correct = 2
total = 3

React
correct = 1
total = 3

SQL
correct = 2
total = 2
```

---

# 46. محاسبه درصد JavaScript

برای JavaScript:

```text
correct = 2
total = 3
```

فرمول:

```text
percentage = (correct / total) × 100
```

جایگذاری:

```text
percentage = (2 / 3) × 100
```

نتیجه:

```text
percentage = 66.666...
```

با `Math.round()`:

```text
percentage = 67
```

پس خروجی JavaScript:

```ts
{
  tag: 'JavaScript',
  correct: 2,
  total: 3,
  percentage: 67
}
```

---

# 47. محاسبه درصد React

برای React:

```text
correct = 1
total = 3
```

فرمول:

```text
percentage = (1 / 3) × 100
```

نتیجه:

```text
33.333...
```

با `Math.round()`:

```text
33
```

پس:

```ts
{
  tag: 'React',
  correct: 1,
  total: 3,
  percentage: 33
}
```

---

# 48. محاسبه درصد SQL

برای SQL:

```text
correct = 2
total = 2
```

فرمول:

```text
percentage = (2 / 2) × 100
```

پس:

```text
percentage = 100
```

بنابراین:

```ts
{
  tag: 'SQL',
  correct: 2,
  total: 2,
  percentage: 100
}
```

---

# 49. نتیجه قبل از مرتب‌سازی

قبل از اجرای `sort` ممکن است نتیجه به شکل زیر باشد:

```text
JavaScript → 67%
React      → 33%
SQL        → 100%
```

---

# 50. اجرای Sort

کد:

```ts
result.sort((a, b) => a.percentage - b.percentage);
```

درصدها:

```text
JavaScript → 67
React      → 33
SQL        → 100
```

از کوچک به بزرگ:

```text
33
67
100
```

بنابراین نتیجه:

```text
React      → 33%
JavaScript → 67%
SQL        → 100%
```

---

# 51. خروجی نهایی مثال

تابع این آرایه را برمی‌گرداند:

```ts
[
  {
    tag: 'React',
    correct: 1,
    total: 3,
    percentage: 33,
  },
  {
    tag: 'JavaScript',
    correct: 2,
    total: 3,
    percentage: 67,
  },
  {
    tag: 'SQL',
    correct: 2,
    total: 2,
    percentage: 100,
  },
]
```

---

# 52. تفسیر خروجی

از این نتیجه می‌توانیم بفهمیم:

```text
React
33%
↓
ضعیف‌ترین مهارت
```

```text
JavaScript
67%
↓
عملکرد متوسط
```

```text
SQL
100%
↓
قوی‌ترین مهارت
```

بنابراین سیستم می‌تواند به دانشجو پیشنهاد دهد:

> بهتر است روی مهارت React تمرکز بیشتری داشته باشید.

---

# 53. یک مثال با Skill Tag خالی

فرض کنیم ورودی این باشد:

```ts
[
  {
    skillTag: 'React',
    isCorrect: true,
  },
  {
    skillTag: null,
    isCorrect: false,
  },
  {
    skillTag: '',
    isCorrect: true,
  },
  {
    skillTag: '   ',
    isCorrect: false,
  },
]
```

سه مورد آخر Skill Tag معتبر ندارند.

بنابراین همه آنها در:

```text
سایر
```

قرار می‌گیرند.

پس:

```text
React
correct = 1
total = 1
percentage = 100
```

و:

```text
سایر
correct = 1
total = 3
percentage = 33
```

---

# 54. چرا گروه `سایر` مفید است؟

اگر Skill Tag وجود نداشته باشد، به جای اینکه اطلاعات آن سؤال حذف شود، تابع آن را در یک گروه عمومی قرار می‌دهد:

```text
سایر
```

بنابراین تمام پاسخ‌ها در تحلیل باقی می‌مانند.

این موضوع باعث می‌شود سؤال‌هایی که هنوز Skill Tag برای آنها تعیین نشده نیز در آمار کلی لحاظ شوند.

---

# 55. پیچیدگی زمانی تابع

اگر تعداد پاسخ‌ها را `n` در نظر بگیریم:

حلقه اول:

```ts
for (const a of answers)
```

هر پاسخ را یک بار بررسی می‌کند.

پس پیچیدگی آن:

```text
O(n)
```

است.

سپس برای Skillهای مختلف یک `sort` انجام می‌شود.

اگر تعداد Skillها را `k` در نظر بگیریم:

```text
O(k log k)
```

خواهد بود.

بنابراین پیچیدگی کلی تقریباً:

```text
O(n + k log k)
```

است.

از آنجا که معمولاً تعداد Skillها خیلی کمتر از تعداد پاسخ‌ها است، این الگوریتم برای چنین کاربردی مناسب است.

---

# 56. تفاوت این تابع با `classifyTrend`

تابع قبلی:

```text
classifyTrend
```

به بررسی **روند نمرات در طول زمان** می‌پرداخت.

یعنی سؤال آن این بود:

> آیا عملکرد دانشجو در آزمون‌های متوالی بهتر شده یا بدتر؟

اما این تابع:

```text
groupBySkill
```

به بررسی **عملکرد دانشجو در مهارت‌های مختلف** می‌پردازد.

یعنی سؤال آن این است:

> دانشجو در کدام مهارت‌ها قوی و در کدام مهارت‌ها ضعیف است؟

بنابراین این دو تابع می‌توانند در کنار هم اطلاعات بسیار مفیدی برای سیستم آموزشی تولید کنند.

---

# 57. ترکیب دو تابع در سیستم LMS

می‌توان از دو تابع برای ساخت یک تحلیل کامل از دانشجو استفاده کرد.

### `classifyTrend`

تحلیل زمانی:

```text
آیا دانشجو در حال پیشرفت است؟
```

خروجی:

```text
صعودی
ثابت
نزولی
```

### `groupBySkill`

تحلیل مهارتی:

```text
دانشجو در چه مهارت‌هایی ضعیف است؟
```

خروجی:

```text
React → 33%
JavaScript → 67%
SQL → 100%
```

بنابراین:

```text
تحلیل دانشجو
       │
       ├── روند کلی یادگیری
       │       ↓
       │    صعودی / ثابت / نزولی
       │
       └── تحلیل مهارت‌ها
               ↓
       نقاط ضعف و قوت
```

---

# 58. کاربرد در سیستم آموزشی

این تابع می‌تواند در LMS برای موارد مختلفی استفاده شود.

مثلاً:

### داشبورد دانشجو

```text
نقاط قوت:
SQL → 100%
JavaScript → 67%

نیازمند تمرین:
React → 33%
```

---

### سیستم پیشنهاد دوره

اگر دانشجو در:

```text
React → 33%
```

ضعیف باشد، سیستم می‌تواند دوره‌های مربوط به React را پیشنهاد دهد.

---

### سیستم پیشنهاد تمرین

سیستم می‌تواند برای مهارت ضعیف‌تر سؤال‌های بیشتری ارائه کند.

مثلاً:

```text
React → 33%
```

پس:

```text
تمرین بیشتر React
```

پیشنهاد شود.

---

### تحلیل عملکرد توسط مدرس

مدرس می‌تواند ببیند:

```text
کدام مهارت‌ها برای دانشجو مشکل‌ساز هستند؟
```

---

# 59. منطق کامل الگوریتم

کل تابع را می‌توان به این شکل خلاصه کرد:

```text
دریافت پاسخ‌های دانشجو
        ↓
بررسی هر پاسخ
        ↓
استخراج Skill Tag
        ↓
اگر Skill Tag خالی بود
        ↓
      "سایر"
        ↓
پیدا کردن گروه مربوط به Skill
        ↓
افزایش total
        ↓
اگر پاسخ درست بود
        ↓
افزایش correct
        ↓
ساخت آمار هر Skill
        ↓
محاسبه درصد موفقیت
        ↓
مرتب‌سازی بر اساس درصد
        ↓
ضعیف‌ترین Skill در ابتدای لیست
```

---

# 60. خلاصه متغیرهای مهم

| متغیر | کاربرد |
|---|---|
| `answers` | لیست پاسخ‌های دانشجو |
| `skillTag` | مهارت مربوط به سؤال |
| `isCorrect` | درست یا غلط بودن پاسخ |
| `map` | ذخیره آمار هر Skill |
| `tag` | نام نهایی Skill |
| `bucket` | آمار یک Skill خاص |
| `correct` | تعداد پاسخ‌های درست |
| `total` | تعداد کل سؤال‌ها |
| `result` | خروجی نهایی |
| `percentage` | درصد موفقیت در Skill |

---

# 61. خلاصه فرمول درصد

فرمول اصلی این تابع:

```text
                 تعداد پاسخ‌های درست
درصد موفقیت = -------------------------- × 100
                 تعداد کل سؤال‌ها
```

مثلاً:

```text
تعداد پاسخ درست = 7
تعداد کل سؤال = 10
```

پس:

```text
درصد = (7 / 10) × 100
```

نتیجه:

```text
70%
```

---

# 62. توضیح خیلی ساده برای فهم شخصی

اگر بخواهیم کل تابع را خیلی ساده توضیح دهیم:

> این تابع پاسخ‌های دانشجو را بر اساس مهارتی که هر سؤال دارد دسته‌بندی می‌کند.

> برای هر مهارت می‌شمارد چند سؤال وجود داشته و دانشجو چند سؤال را درست جواب داده است.

> بعد با تقسیم تعداد پاسخ‌های درست بر تعداد کل سؤال‌ها، درصد موفقیت دانشجو در آن مهارت را محاسبه می‌کند.

> در پایان مهارت‌ها را از ضعیف‌ترین به قوی‌ترین مرتب می‌کند تا سیستم بتواند سریع‌تر نقاط ضعف دانشجو را پیدا کند.

---

# 63. متن مناسب برای جلسه دفاع

اگر در جلسه دفاع از شما پرسیده شد:

**«تابع `groupBySkill` چه کاری انجام می‌دهد؟»**

می‌توانید این‌طور توضیح دهید:

> این تابع برای تحلیل عملکرد دانشجو بر اساس مهارت‌های مختلف طراحی شده است. هر سؤال در سیستم دارای یک Skill Tag است که مشخص می‌کند سؤال مربوط به چه مهارتی است و همچنین مشخص است که پاسخ دانشجو به آن سؤال درست بوده یا غلط. تابع ابتدا پاسخ‌ها را بر اساس Skill Tag گروه‌بندی می‌کند و برای هر مهارت تعداد کل سؤالات و تعداد پاسخ‌های صحیح را محاسبه می‌کند. سپس با استفاده از نسبت پاسخ‌های صحیح به کل سؤالات، درصد موفقیت دانشجو در هر مهارت محاسبه می‌شود. در نهایت، نتایج بر اساس درصد موفقیت از کمترین به بیشترین مرتب می‌شوند تا ضعیف‌ترین مهارت‌های دانشجو در ابتدای لیست قرار بگیرند و سیستم بتواند برای آنها پیشنهاد آموزشی یا تمرین بیشتری ارائه کند.

---

# 64. اگر استاد درباره `Map` سؤال کرد

اگر پرسید:

**«چرا از Map استفاده کردید؟»**

پاسخ:

> چون نیاز داشتیم برای هر Skill Tag یک Bucket مستقل از آمار داشته باشیم. `Map` این امکان را می‌دهد که بر اساس نام Skill، آمار مربوط به آن را به صورت سریع پیدا کنیم و تعداد پاسخ‌های صحیح و کل پاسخ‌ها را به‌روزرسانی کنیم.

به زبان ساده:

```text
Skill Tag
   ↓
پیدا کردن آمار همان Skill
   ↓
به‌روزرسانی correct و total
```

---

# 65. اگر استاد درباره `trim()` سؤال کرد

اگر پرسید:

**«چرا از trim استفاده کردید؟»**

پاسخ:

> برای حذف فاصله‌های اضافی ابتدا و انتهای Skill Tag استفاده شده است. این کار باعث می‌شود مقادیر مانند `"React"` و `"  React  "` به عنوان دو مهارت متفاوت ثبت نشوند.

مثلاً بدون `trim`:

```text
"React"
" React"
"React "
```

ممکن است سه مقدار متفاوت محسوب شوند.

اما با `trim()`:

```text
"React"
" React"
"React "
```

همه به:

```text
"React"
```

تبدیل می‌شوند.

---

# 66. اگر استاد درباره `Math.round()` سؤال کرد

پاسخ:

> چون نتیجه تقسیم ممکن است اعشاری باشد، از `Math.round()` استفاده شده تا درصد نهایی به نزدیک‌ترین عدد صحیح گرد شود.

مثلاً:

```text
66.666...
```

تبدیل می‌شود به:

```text
67
```

---

# 67. اگر استاد درباره مرتب‌سازی سؤال کرد

اگر پرسید:

**«چرا از ضعیف‌ترین به قوی‌ترین مرتب کردید؟»**

پاسخ:

> هدف اصلی از این مرتب‌سازی، اولویت دادن به نقاط ضعف دانشجو است. با قرار گرفتن کمترین درصد در ابتدای آرایه، سیستم یا مدرس می‌تواند سریع‌تر مهارت‌هایی را که نیاز به تمرین بیشتری دارند شناسایی کند.

---

# 68. نتیجه نهایی

تابع `groupBySkill` یک ابزار برای **تحلیل مهارتی عملکرد دانشجو** است.

فرآیند آن:

```text
پاسخ‌های دانشجو
       ↓
گروه‌بندی بر اساس Skill
       ↓
محاسبه تعداد کل سؤالات
       ↓
محاسبه تعداد پاسخ‌های درست
       ↓
محاسبه درصد موفقیت
       ↓
مرتب‌سازی از ضعیف‌ترین به قوی‌ترین
       ↓
شناسایی نقاط ضعف و قوت دانشجو
```

در مثال ما:

```text
React      → 33%  ← ضعیف‌ترین
JavaScript → 67%
SQL        → 100% ← قوی‌ترین
```

بنابراین سیستم می‌تواند نتیجه بگیرد:

```text
نقطه ضعف:
React

نقطه قوت:
SQL
```

و از این اطلاعات برای:

- پیشنهاد دوره
- پیشنهاد تمرین
- تولید محتوای آموزشی شخصی‌سازی‌شده
- تحلیل عملکرد دانشجو
- نمایش داشبورد آموزشی
- کمک به مدرس برای شناسایی نقاط ضعف

استفاده کند.

---

# 69. خلاصه یک‌جمله‌ای برای حفظ کردن

> تابع `groupBySkill` پاسخ‌های دانشجو را بر اساس مهارت دسته‌بندی می‌کند، درصد موفقیت در هر مهارت را محاسبه می‌کند و مهارت‌ها را از ضعیف‌ترین به قوی‌ترین مرتب می‌کند تا نقاط ضعف و قوت دانشجو مشخص شوند.