# راهنمای حل مشاکل - آزمون‌های تمرینی با هوش مصنوعی

## 🔴 مشکل: وقتی "شروع تمرین" میزنم هیچ اتفاقی نمی‌فته

### ✅ حل: سرور رو روشن کن

```bash
# Terminal رو باز کن و این رو اجرا کن:
cd backend
npm run start

# یا برای mode development:
npm run dev
```

**بعد از اجرا، باید ببینی:**
```
[Nest] 12345  - 12/08/2027, 10:30:00 AM     LOG [NestFactory] Starting Nest application...
[Nest] 12345  - 12/08/2027, 10:30:02 AM     LOG [InstanceLoader] TypeOrmModule dependencies initialized
```

---

## 🔴 مشکل 2: سرویس هوش مصنوعی در دسترس نیست

### این پیغام ها رو می‌بینی؟
```
❌ سرویس هوش مصنوعی در دسترس نیست. لطفاً بعداً دوباره تلاش کنید.
```

### ✅ حل: Environment variable رو تنظیم کن

**Step 1: فایل `.env` رو باز کن**
```
backend/.env
```

**Step 2: این دو خط رو اضافه کن:**
```env
AI_API_URL=http://92.246.145.99:1234/v1/chat/completions
AI_MODEL_NAME=qwen/qwen3-4b
```

**Step 3: سرور رو دوباره شروع کن**
```bash
npm run start
```

---

## 🔴 مشکل 3: خطای 503 - سرویس موقتاً در دسترس نیست

### پیغام:
```
❌ سرویس هوش مصنوعی موقتاً در دسترس نیست. لطفاً بعداً دوباره تلاش کنید.
```

### ✅ این یعنی:
- سرور Qwen API shutdown است
- یا overload شده
- یا اینترنت قطع شده

### حل:
1. **چند دقیقه صبر کن**
2. **دوباره تلاش کن**
3. **اگر ادامه داشت:**
   - اینترنت رو چک کن
   - آدرس API رو بررسی کن

---

## 🔴 مشکل 4: خطای 401 - احراز هویت ناموفق

### پیغام:
```
❌ خطای احراز هویت سرویس هوش مصنوعی. لطفاً تنظیمات سرور را بررسی کنید.
```

### ✅ حل:
- `AI_API_URL` درست تنظیم شده؟
- فایل `.env` درست ذخیره شده؟
- سرور رو restart کردی بعد از تغییر `.env`؟

---

## 🔴 مشکل 5: هیچ سوال تولید نشد

### پیغام:
```
❌ هوش مصنوعی نتوانست سوالات معتبری تولید کند. لطفاً دوباره تلاش کنید.
```

### ✅ دلایل ممکن:
1. **دوره metadata ناقص:** دوره شرح مختصر ندارد
2. **مهارت غلط:** نام مهارت اشتباه است
3. **مشکل هوش مصنوعی:** Qwen API خرابی دارد

### حل:
1. **دوره رو چک کن:**
   - عنوان دوره موجود است؟
   - توضیح کوتاه نوشته شده؟
   - مهارت‌های یادگیری تعریف شده؟

2. **دوباره تلاش کن:**
   ```
   یا skill تغییر بده
   یا question count کاهش بده
   ```

---

## 🟡 مشکل 6: سوالات تولید شد اما بسیار بد است

### ✅ این طبیعی است!

هوش مصنوعی شاید:
- سوال خیلی سخت بسازه
- سوال خیلی آسان بسازه
- متن سوال نامناسب باشه
- برچسب مهارت اشتباه باشه

### حل:
1. **دوباره تلاش کن** - هر بار متفاوت است
2. **تعداد سوالات کاهش بده** - کیفیت بهتر است
3. **skill tag واضح‌تر بده** - مثلاً "حلقه های تکرار" نه "حلقه"

---

## 📊 چگونه مشکل رو تشخیص بدهم؟

### 1️⃣ Console رو باز کن (F12)

```
Chrome:     F12 → Console
Firefox:    F12 → Console
Edge:       F12 → Console
Safari:     Cmd+Option+I → Console
```

### 2️⃣ "شروع تمرین" رو کلیک کن

### 3️⃣ Exact error message رو بخون

### 4️⃣ اینجا دنبال بگرد:

| پیغام | معنی | حل |
|------|------|-----|
| `سرویس در دسترس نیست` | Qwen API down است | صبر کن |
| `خطای احراز هویت` | API URL غلط است | `.env` چک کن |
| `صل Connection refused` | سرور NestJS down است | `npm run start` |
| `هیچ سوال تولید نشد` | دوره metadata ناقص | توضیح دوره اضافه کن |
| `JSON parse error` | API غریب جوابی داد | دوباره تلاش کن |

---

## 🟢 دقیق‌ترین راه تشخیص: Network Tab

### Step 1: F12 → Network

### Step 2: "شروع تمرین" کلیک کن

### Step 3: درخواست رو جستجو کن:
```
POST .../practice-exams/courses/1/generate
```

### Step 4: جواب رو ببین:

```json
// اگر مشکلی نیست:
[
  {
    "id": -1,
    "questionText": "سوال",
    "skillTag": "مهارت",
    "choices": [...]
  }
]

// اگر خطا:
{
  "statusCode": 400,
  "message": "❌ سرویس هوش مصنوعی در دسترس نیست. لطفاً بعداً دوباره تلاش کنید.",
  "error": "Bad Request"
}
```

---

## 🔧 Backend Logs رو چک کن

**Terminal که backend روشن است رو ببین:**

```bash
# اگر ببینی:
[Nest] Listening on port 3000

# ✅ خوب است - سرور کار می‌کند

# اگر خطا ببینی:
Error: Cannot find module 'ai'
Error: AI_API_URL not defined

# ❌ مشکل دارد - لازم تغییر است
```

---

## ✅ تمام‌یابی Checklist

```
□ سرور NestJS روشن است؟
  npm run start
  
□ دوره metadata کامل است؟
  - عنوان دارد
  - توضیح کوتاه دارد
  - Learning outcomes تعریف شده
  
□ Environment variables تنظیم شده؟
  - AI_API_URL موجود
  - AI_MODEL_NAME موجود
  
□ Skill tag صحیح است؟
  - 2-4 کلمه فارسی
  - مثل: "حلقه های تکرار"
  
□ Question count معقول است؟
  - کمتر از 20
  - بیشتر از 0
  
□ Qwen API accessible است؟
  - اینترنت متصل است
  - URL صحیح است
```

---

## 🆘 اگر هنوز مشکل دارد

### اطلاعات رو برام ارسال کن:

1. **دقیق error message**
   ```
   چه آیکون قرمز رنگ (❌) نشون میده؟
   ```

2. **Console error log**
   ```
   F12 → Console → عکس بگیر
   ```

3. **Network response**
   ```
   F12 → Network → POST request → Response بخش
   ```

4. **Backend logs**
   ```
   Terminal که sever روشن است
   چی می‌نویسه؟
   ```

5. **Environment setup**
   ```
   .env فایل تو backend folder هست؟
   ```

با این اطلاعات می‌تونم دقیق مشکل رو حل کنم! 🔧

---

## 📝 خلاصه

| مشکل | علامت | حل |
|------|-------|-----|
| سرور off | هیچ اتفاقی نمی‌فته | `npm run start` |
| API قطع | خطای موقتی | صبر و دوباره تلاش |
| Environment نیست | خطای authentication | `.env` چک کن |
| دوره ناقص | هیچ سوال | metadata اضافه کن |
| مشکل JSON | parse error | دوباره تلاش |

**بیشتر مشاکل با restart سرور حل میشن!** 🚀

---

**نوشته شده**: دسامبر 8، 1402
**برای**: Learning Management System - Practice Exams
**زبان**: فارسی 🇮🇷
